# Normaliza quebras de linha como LF (código roda em containers Linux).
* text=auto eol=lf

# Binários não devem sofrer normalização.
*.png binary
*.jpg binary
*.jpeg binary
*.pdf binary
*.ico binary
